info:
  name: View All Tickets
  type: http
  seq: 2

http:
  method: GET
  url: "{{cs_url}}/customer-service"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
