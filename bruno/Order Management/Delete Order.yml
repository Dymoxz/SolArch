info:
  name: Delete Order
  type: http
  seq: 4

http:
  method: DELETE
  url: "{{base_url}}/orders/{{order_id}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
