info:
  name: List Payments
  type: http
  seq: 5

http:
  method: GET
  url: "{{pa_url}}/payments"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
