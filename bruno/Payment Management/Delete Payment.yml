info:
  name: Delete Payment
  type: http
  seq: 4

http:
  method: DELETE
  url: "{{pa_url}}/payments/{{paymentId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
