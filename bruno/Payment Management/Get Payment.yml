info:
  name: Get Payment
  type: http
  seq: 2

http:
  method: GET
  url: "{{pa_url}}/payments/{{paymentId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
