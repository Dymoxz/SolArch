info:
  name: Get Shipments
  type: http
  seq: 1

http:
  method: GET
  url: "{{shipping_url}}/shipping/shipments"
  body: none
  auth: inherit

runtime:
  variables:
    - name: shipping_url
      value: http://localhost:8002
