info:
  name: Get Shipments
  type: http
  seq: 1

http:
  method: GET
  url: "{{shipping_url}}/shipping/shipments"
  auth: inherit

runtime:
  variables:
    - name: shipping_url
      value: http://localhost:8003

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
