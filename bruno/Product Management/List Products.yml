info:
  name: List Products
  type: http
  seq: 5

http:
  method: GET
  url: "{{pd_url}}/products"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
