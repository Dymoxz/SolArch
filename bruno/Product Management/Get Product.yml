info:
  name: Get Product
  type: http
  seq: 2

http:
  method: GET
  url: "{{pd_url}}/products/{{productId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
